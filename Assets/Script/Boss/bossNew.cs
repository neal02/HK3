using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bossNew : MonoBehaviour //보스의 본체 스크립트, 각자 트리거에 따른 스크립트는 자식 오브젝트에서 구현
{
    void FixedUpdate()
    {
        if(isDetecting)  //보스전이 시작되면 우선 플레이어 위치 탐색 시작
        {
            float distace; //플레이어와 보스의 x축 거리

            
        }
    }
}
